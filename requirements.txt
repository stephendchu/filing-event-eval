# Core
anthropic>=0.40
python-dotenv>=1.0
httpx>=0.27

# Observability (the headline: Arize Phoenix + OpenTelemetry)
arize-phoenix>=5.0
openinference-instrumentation-anthropic>=0.1
opentelemetry-sdk>=1.27

# Dev
pytest>=8.0

# --- deferred to later slices (not used yet; keeps the install light) ---
# chromadb>=0.5             # vector RAG slice (cross-filing / query tasks)
# sentence-transformers>=3.0
# langgraph>=0.2            # multi-node orchestration slice
