# Core
anthropic>=0.40
python-dotenv>=1.0
httpx>=0.27

# Retrieval / embeddings (only when vector RAG is justified)
chromadb>=0.5
sentence-transformers>=3.0

# Observability (the headline: Arize Phoenix + OpenTelemetry)
arize-phoenix>=5.0
openinference-instrumentation-anthropic>=0.1
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27

# Orchestration (added in the LangGraph upgrade slice)
# langgraph>=0.2

# Dev
pytest>=8.0
